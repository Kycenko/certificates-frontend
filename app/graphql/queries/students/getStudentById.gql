query getStudentById($id: String!) {
	getStudentById(id: $id) {
		id
		lastName
		firstName
		secondName
		birthDate
		groupId
		isExpelled
		certificates {
			id
			startDate
			finishDate
		}
		group {
			id
			title
		}
	}
}
