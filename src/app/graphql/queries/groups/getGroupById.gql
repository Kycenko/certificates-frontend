query getGroupById($id: String!) {
	getGroupById(id: $id) {
		id
		title
		curator {
			displayedName
		}
		students {
			id
			lastName
			firstName
			secondName
			birthDate

			certificates {
				id
				startDate
				finishDate
			}
		}
		course {
			id
			number
			departmentId
		}
	}
}
